name: Reusable Deploy #Should work with use-reusable - check line 71 for correct path and filename
on: workflow_call
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Output Information
        run: echo "Deploying & Uploading..."
